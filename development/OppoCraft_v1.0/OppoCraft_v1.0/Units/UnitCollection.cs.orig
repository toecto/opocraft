<<<<<<< HEAD:development/OppoCraft_v1.0/OppoCraft_v1.0/UnitCollection.cs
﻿using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace OppoCraft
{
    public class UnitCollection : Dictionary<int, Unit>
    {
        public Unit getById(int id)
        {
            if (this.ContainsKey(id))
                return this[id];
            return null;
        }

        new public void Remove(int id)
        {
            Unit u = this.getById(id);
            if (u != null)
                base.Remove(id);
        }

        public void Remove(Unit u)
        {
            base.Remove(u.id); 
        }

        public virtual void Add(Unit u)
        {
            this.Add(u.id, u);
        }

    }
    
}
=======
﻿using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace OppoCraft
{
    public class UnitCollection : Dictionary<int, Unit>
    {
        public Unit getById(int id)
        {
            if (this.ContainsKey(id))
                return this[id];
            return null;
        }

        new public void Remove(int id)
        {
            Unit u = this.getById(id);
            if (u != null)
                this.Remove(u);
        }

        public void Remove(Unit u)
        {
            this.Remove(u.id); 
        }

        public virtual void Add(Unit u)
        {
            this.Add(u.id, u);
        }

    }
    
}
>>>>>>> ddd71582b5bef7347bb44463313fc02b4c423eb2:development/OppoCraft_v1.0/OppoCraft_v1.0/Units/UnitCollection.cs
